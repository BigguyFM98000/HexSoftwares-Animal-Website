// Smooth scrolling function
function smoothScroll(target) {
    document.querySelector(target).scrollIntoView({
        behavior: 'smooth'
    });
}

// Alert function for donation buttons
function donateAlert() {
    alert("Thank you for your generosity!");
}
